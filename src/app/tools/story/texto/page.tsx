"use client";

import { AppChrome } from "@/components/SiteChrome";
import { StoryTextTemplate } from "@/components/StoryTextTemplate";

export default function StoryTextToolPage() {
  return (
    <AppChrome>
      <StoryTextTemplate />
    </AppChrome>
  );
}
